import { StyleSheet } from 'react-native';

export const SwipeCardStyleSheet = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    position: 'relative',
  },
  card: {
    width: '100%',
    height: '100%',
    position: 'absolute',
  },
});
